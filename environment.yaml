channels:
  - conda-forge
dependencies:
  # dependencies
  - xtensor
  - goosefem
  - qpot

  # build tools
  - cmake
  - ninja
  - setuptools_scm

  # Python API
  - python
  - numpy
  - pybind11
  - xtensor-python

  # run tests
  - catch2
  - prrng
  - python-qpot

  # run examples
  - fmt
  - highfive

  # build docs
  - doxygen
  - mathjax
